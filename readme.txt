readme test
git 只能管理纯文本，是一个分布式的版本控制系统，但是一般有一个中央管理服务器。
git init
git add .
git commit -m "xxx"
git log
git log --pretty=oneline
git reset --hard HEAD^
git checkout -- (filename)
git reset HAED (filename)
git rm (filename)



1.工作区：本地电脑目录
2.版本库：。git，包括暂存区，分支和HEAD指针
3.暂存区：stage，git add就是进入此处。    git commit 是进入分支


